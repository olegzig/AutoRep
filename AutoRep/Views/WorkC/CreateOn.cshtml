@model AutoRep.Models.Work

@{
    ViewData["Title"] = "Create";
}

<h1>Добавить</h1>

<h4>Работа</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Client" class="control-label">Контакты клиента</label>
                <input asp-for="Client" class="form-control" value="@ViewBag.Name" />
                <span asp-validation-for="Client" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Date" class="control-label">Дата</label>
                <input asp-for="Date" class="form-control" />
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Worker" class="control-label">Работник</label>
                <div>@Html.DropDownListFor(x => x.Worker, new SelectList(ViewBag.UsersBag, "Id", "UserName"), new { @class = "form-control" })</div>
                <span asp-validation-for="Worker" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkType" class="control-label">Оказываемая услуга</label>
                <div>@Html.DropDownListFor(x => x.WorkType, new SelectList(ViewBag.WorkTypeBag, "Id", "Name"), new { @class = "form-control" })</div>
                <span asp-validation-for="WorkType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Добавить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}